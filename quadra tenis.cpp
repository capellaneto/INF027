#include <stdio.h>
#include <stdlib.h>
int main ()
{
	int k, a, L, x;
	printf("digite o valor de k:");
	scanf("%d", &k);
	
	a=k;
	L=2*k;
	
	printf("digite a coordenada x,y que a bola caiu:");
	scanf("%d,%d", &L,&a);
	//transformando em X
	if(L>0 && L<=k)
	L=L*(-1);
	
	//transformando em y
	
	#include <stdio.h>
#include <stdlib.h>
int main ()
{
	int k, a, L, x;
	printf("digite o valor de k:");
	scanf("%d", &k);
	
	a=k;
	L=2*k;
	
	printf("digite a coordenada x,y que a bola caiu:");
	scanf("%d,%d", &L,&a);
	//transformando em X
	if(L>0 && L<=k)
	L=L*(-1);
	
	//transformando em y
	if(a>k && 2*k>=a)
	x=a/2;
	
	printf("%d,%d", L,x);
}
	
	printf("%d,%d", L,x);
}